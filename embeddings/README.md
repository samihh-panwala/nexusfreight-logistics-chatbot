# Embeddings

This folder is reserved for storing vector embeddings generated from the logistics knowledge base.

These embeddings will be created using an embedding model and stored in a vector database or supported storage system.

The chatbot will use these embeddings to perform semantic search and retrieve the most relevant information before generating responses.


# Purpose: Explains why the embeddings folder exists and how it will be used in a future RAG mplementation.